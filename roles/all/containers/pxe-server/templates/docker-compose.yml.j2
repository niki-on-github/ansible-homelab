version: '3.7'

services:
  pxe_server:
    build:
      context: ./container
      args:
        NETWORK_IP: "{{ network_ip }}"
    container_name: pxe-server
    restart: unless-stopped
    network_mode: 'host'
    cap_add:
      - NET_ADMIN

